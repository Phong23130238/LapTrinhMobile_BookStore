<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Màu nền của hộp thoại (Màu trắng) -->
    <solid android:color="#FFFFFF" />
    <!-- Độ bo tròn các góc -->
    <corners android:radius="16dp" />
</shape>