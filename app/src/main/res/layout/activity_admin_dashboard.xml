<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_light"
    android:gravity="center_horizontal">

    <!-- Phần Header (Từ nhánh main) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/navy_dark"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingTop="36dp"
        android:paddingBottom="36dp"
        android:elevation="8dp">
        
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Dashboard"
            android:textSize="26sp"
            android:textStyle="bold"
            android:letterSpacing="0.05"
            android:textColor="@color/white"/>
            
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Quản trị hệ thống BookStore"
            android:textSize="14sp"
            android:layout_marginTop="4dp"
            android:textColor="@color/gold"/>
    </LinearLayout>

    <!-- Phần Menu cuộn -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">
        
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">
            
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnManageBooks"
                style="@style/NavyButton"
                android:text="Quản lý Sản phẩm (Sách)"
                android:layout_marginBottom="12dp"/>
                
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnManageOrders"
                style="@style/NavyButton"
                android:text="Quản lý Đơn hàng"
                android:layout_marginBottom="12dp"/>
                
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnManageInventory"
                style="@style/NavyButton"
                android:text="Quản lý Kho (Nhập / Xuất)"
                android:layout_marginBottom="12dp"/>
                
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnManageUsers"
                style="@style/NavyButton"
                android:text="Quản lý Người dùng"
                android:layout_marginBottom="12dp"/>

            <!-- Nút mới lấy từ nhánh Phat, đã được chuẩn hóa Style -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAdminStats"
                style="@style/NavyButton"
                android:text="Thống kê Kinh doanh"
                android:layout_marginBottom="24dp"/>
                
            <Button
                android:id="@+id/btnAdminLogout"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Đăng xuất khỏi hệ thống"
                android:textColor="@color/red_danger"/>
                
        </LinearLayout>
    </ScrollView>
</LinearLayout>